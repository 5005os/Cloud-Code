/* ============ AI KYRGYZSTAN — конфигурация ============
 *
 * Заполни эти значения, чтобы включить ВХОД ПО ПОЧТЕ и общий ИИ для всех пользователей.
 * Пока значения пустые — сайт работает в открытом режиме (без входа).
 *
 * 1) SUPABASE_URL и SUPABASE_ANON_KEY — бесплатно на https://supabase.com
 *    (Project Settings → API). anon key публичный, его можно показывать.
 *
 * 2) AI_KEY — (необязательно) бесплатный ключ ИИ, чтобы пользователям
 *    НЕ нужно было вводить ключ самим. Получить: https://aistudio.google.com/app/apikey
 */
window.AIKG_CONFIG = {
  SUPABASE_URL: "",       // ← вставь Project URL
  SUPABASE_ANON_KEY: "",  // ← вставь anon (public) key
  AI_KEY: ""              // ← (необязательно) бесплатный ключ ИИ для всех
};
